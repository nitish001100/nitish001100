import com.springerNature.sourceExtractor.FileSource;
import org.junit.jupiter.api.Test;

public class FileSourceJUnit {

    @Test
    public void sourceTest() {
        FileSource fileSource = new FileSource();
        /**
         1: Mock the the file source object by mocking the method, so that we can we have object.
         2: After that keep the actual file source and expected file source in target and do asserts between them.
         */
    }
}
